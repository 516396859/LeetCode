public class twoPoint {

    public void samedirection(int arr[]) {

        // 思想：两个指针，左指针先固定，右指针右滑，满足条件就处理
        // 当右指针遇到不满足条件的下标则右指针不动，滑动左指针，直到遇见左指针满足条件的
        // 总结：右指针满足条件一直滑动，直到遇见不满足的区间停止，此时滑动左指针；
        //      左指针不满足条件一直滑动，直到遇见满足的区间停止，此时滑动右指针；

        int left = 0; //左指针
//        for (int right = 0; right < arr.length; right++) { //右指针右滑
//
//            while (left < arr.length && arr[left,right]不满足条件){
//                left++;
//            }
//
//            if (arr[left,right] 满足条件){
//                //对此区间进行处理
//            }
//
//        }
    }

}
